# Example environment file for the MagicScool frontend.

# Base URL for API requests. During development this typically
# points to your locally running backend. When deploying to
# production (e.g. Cloudflare Pages) you can override this with a
# Pages Function proxy or use the fully qualified backend origin.
VITE_API_BASE_URL=http://localhost:5000/api