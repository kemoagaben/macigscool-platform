# Production environment overrides for the MagicScool frontend.
#
# Set VITE_API_BASE_URL to the URL of your backend API when
# deploying outside of Cloudflare Pages (e.g. directly to a VM or
# container). If using Pages Functions as a reverse proxy, leave
# this unset and configure BACKEND_ORIGIN in your Cloudflare Pages
# settings.

#VITE_API_BASE_URL=https://api.example.com/api